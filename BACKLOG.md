# Backlog

Not scheduled — noted for later, not being built now. See `MASTER_PROMPT.md` for what's actually in scope for the current build.

## Onboarding screen (replaces the current bare `/` home page)

Current `/` is just the logo, name, tagline, and a "Start writing" button — no explanation of what the product does. Replace it with a real onboarding/landing page structured as:

1. **Problem statement** — the copy-paste-between-ChatGPT-and-your-thesis pain, fabricated citations (see `MASTER_PROMPT.md` > "Kenapa produk ini dibuat")
2. **Bridging** — how Reindex closes that gap
3. **"Why use Reindex?"** — positioning vs. generic AI writing tools / bare Google Docs
4. **Feature showcase** — the four core features (editor, sidebar agent, selected-text edit, autocomplete)
5. **Get started** — CTA into sign-in/dashboard

Keep the academic-writing positioning throughout (per `CLAUDE.md` > Product Alignment) — not generic SaaS landing-page copy.
